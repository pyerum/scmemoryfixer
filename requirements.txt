# Snapchat Memory Fixer Dependencies
# Core dependencies
Pillow>=9.0.0  # For image overlay operations
opencv-python-headless>=4.8.0  # For video processing
python-dateutil>=2.8.2  # For date parsing

# Note: exiftool is bundled with the application in ./exiftool/
# No need to install it separately

# For packaging
# pyinstaller>=5.0.0
